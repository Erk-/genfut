[package]
name = "{libname}"
description = "{description}"
version = "{version}"
authors = ["{author}"]
edition = "2018"
license = "{license}"

[dependencies]

[build-dependencies]
cc = "1.0"

[features]
default = ["{backend}"]
c = []
multicore = []
ispc = []
cuda = []
opencl = []
